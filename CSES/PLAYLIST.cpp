#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n;
ll a[200005];
void solve(){
	
}
int main () {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	freopen("DEBUG.INP", "r", stdin);
	freopen("DEBUG.OUT", "w", stdout);

	scanf("%lld", &n);
	for (ll i = 0; i < n; ++i)
		scanf("%lld", &a[i]);
	solve();
	return 0;
}